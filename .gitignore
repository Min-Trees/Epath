# Dependencies
node_modules/
.pnp/
.pnp.js

# Testing
coverage/

# Next.js
.next/
out/
build/

# Production
dist/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
env-vercel

# Local runtime data (chat logs, temp caches)
/.data/
/tmp/

# Vercel
.vercel

# Legacy subfolder (kept around for IDE index locks; ignore to avoid committing)
epath-website/

# Firebase CLI cache + local project alias
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Firebase Admin SDK service-account JSON keys (NEVER commit).
# Generate per-developer via Firebase Console -> Project Settings ->
# Service Accounts -> "Generate new private key". Each dev keeps their
# own copy locally and exports its values into .env.local instead.
*-firebase-adminsdk-*.json
serviceAccountKey.json
service-account*.json
gcp-credentials*.json

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE
.idea/
.vscode/
*.swp
*.swo

# Session reports (may contain secrets)
SESSION_REPORT*.md

# Large game files
*.yyz
"A platformer game 2.yyz"
